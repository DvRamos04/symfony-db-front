# symfony-db-front (Frontend)
- Symfony + Twig.
- Lee MySQL (etl_backend) y muestra:
  - Dropdown (ejecución)
  - Pie (género) + Barras (rangos de edad)
  - Tabla (SO)

## Uso
1) `composer install`
2) `copy .env.example .env`
3) Servidor embebido:
   `php -S 127.0.0.1:8001 -t public`
